# ─────────────────────────────────────────────────────────────────────────────
# WeatherGPT Requirements
# ─────────────────────────────────────────────────────────────────────────────

# Backend — FastAPI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# HTTP
requests>=2.31.0
httpx>=0.27.0

# LLM — Google Gemini (new SDK with Client API)
google-genai>=1.0.0

# Cache (Redis, optional — degrades gracefully if unavailable)
redis>=5.0.0

# Static file serving
aiofiles>=23.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0